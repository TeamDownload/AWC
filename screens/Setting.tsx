import React from "react";
import { Text, View, Pressable } from "react-native";
import Footer from "../components/Footer";
const Setting = ({ navigation }: any) => {
  return <Footer Screen="Setting"></Footer>;
};

export default Setting;
